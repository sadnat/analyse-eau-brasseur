{
  "name": "eau-app",
  "version": "1.0.0",
  "description": "Application de visualisation des données sur l'eau pour brasseurs",
  "scripts": {
    "build": "mkdir -p dist && cp -r index.html app.js dist/"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
