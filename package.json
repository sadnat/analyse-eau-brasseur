{
  "name": "eau-app",
  "version": "1.0.0",
  "description": "Application de visualisation des données sur l'eau",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "mkdir -p dist && cp -r index.html app.js dist/"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
